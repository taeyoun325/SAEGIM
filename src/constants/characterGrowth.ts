// "새김"을 계속할수록 함께 자라는 펫. 모든 계정에서 쓸 수 있는 기능이다.

// 모든 종(constants/characterSpecies.ts)이 같은 성장 속도를 쓴다.
// 최종 진화(5단계) 기준을 새긴 생각 60개로 잡고, 중간 단계는 기존 비율
// (0/3/10/30/60/100 → 최종 100 기준)을 그대로 유지한 채 60에 맞게 축소했다.
export const STAGE_THRESHOLDS: number[] = [0, 2, 6, 18, 36, 60];
